import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    BodyContainer: {
        flexGrow: 1,
        width: '100%',
        paddingHorizontal: 5
    },
    FlatListStyle: {
        flexGrow: 1,
        marginBottom: 50
    }
})

export default styles